<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Month_Year_Formula__c</fullName>
    <description>Converts the Date Submitted field to a Month and Year so it can be validated via the GMS Funding Drawdown Request flow.</description>
    <externalId>false</externalId>
    <formula>TEXT(MONTH( Date_Submitted__c )) &amp; TEXT(YEAR(Date_Submitted__c ))</formula>
    <inlineHelpText>Converts the Date Submitted field to a Month and Year so it can be validated via the GMS Funding Drawdown Request flow.</inlineHelpText>
    <label>Month/Year Formula</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
